#include <stdio.h>
/**
 * main - Entry point
 *
 * Discreption: A C program that prins numbers.
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int num = 0;

	for (; num < 10; num++)
		printf(num);
	printf("\n");
	return (0);
}
