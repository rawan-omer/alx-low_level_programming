#include "main.h"

/**
 * puts_half - function that prints half of a string
 * @str: input string
*/

void puts_half(char *str)
{
	int i;

	for (i = 0; s[i] != '0'; i++)
		;
	i++
	for (i /= 2; s[i] != '0'; i++)
		_putchar(str[i]);
	_putchar('\n');
}
