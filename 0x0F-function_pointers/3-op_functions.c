#include "3-calc.h"

/**
 * op_add - add to numbers
 * @a: firest number
 * @b: second number
 * Return: the result
*/
int op_add(int a, int b)
{
	return (a + b);
}
/**
 * op_sub - sub to numbers
 * @a: firest number
 * @b: second number
 * Return: the result
*/
int op_sub(int a, int b)
{
	return (a - b);
}
/**
 * op_mul - mul to numbers
 * @a: firest number
 * @b: second number
 * Return: the result
*/
int op_mul(int a, int b)
{
	return (a * b);
}
/**
 * op_div - div to numbers
 * @a: firest number
 * @b: second number
 * Return: the result
*/
int op_div(int a, int b)
{
	return (a / b);
}
/**
 * op_mod - mod to numbers
 * @a: firest number
 * @b: second number
 * Return: the result
*/
int op_mod(int a, int b)
{
	return (a % b);
}
